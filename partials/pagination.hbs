{{!-- Renders nothing when everything fits on one page. --}}
{{#if pagination.next}}
    {{> "pagination-nav"}}
{{else}}
    {{#if pagination.prev}}
        {{> "pagination-nav"}}
    {{/if}}
{{/if}}
